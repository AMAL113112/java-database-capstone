version: "3.8"

services:

  mongo:
    image: mongo:7
    container_name: smartclinic-mongo
    ports:
      - "27017:27017"

  app:
    build: .
    container_name: smartclinic-app
    ports:
      - "8080:8080"
    depends_on:
      - mongo